# PostgreSQL Deployment
# Required by product-catalog service!
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: otel-demo
  labels:
    app: postgres
    environment: production
    team: platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
      - name: postgres
        image: postgres:16
        ports:
        - containerPort: 5432
        env:
        - name: POSTGRES_USER
          value: "astronomy_user"
        - name: POSTGRES_PASSWORD
          value: "astronomy_pass"
        - name: POSTGRES_DB
          value: "astronomy_db"
        resources:
          requests:
            memory: "128Mi"
            cpu: "250m"
          limits:
            memory: "256Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: otel-demo
  labels:
    app: postgres
spec:
  selector:
    app: postgres
  ports:
  - port: 5432
    targetPort: 5432
